describe('profile creation', () => {
  it('create a user profile with default preferences', () => {
    cy.get('div').then(() => expect(false).to.be.true);
  });

  it('only allow profile creation when signing up new user', () => {
    cy.get('div').then(() => expect(false).to.be.true);
  });
});
