import { fireApp } from './fire-app.js';
import { bind, get, add, set, del } from './fire-store';
import auth from './fire-auth.js';
import { startFireUi } from './fire-ui.js';

export { fireApp, auth, startFireUi, bind, get, add, set, del };
