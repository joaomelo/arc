describe('identity provider module', () => {
  test('signs in and returns correct object', () => {
    expect(true).toBe(false)
  })

  test('throws if attempt sign in with incorrect credentials', () => {
    expect(true).toBe(false)
  })
})
